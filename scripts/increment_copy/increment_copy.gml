/// @function increment_copy(array)
/// @param {array} array
/// @description Add one to every value in the array. Array must only contain numbers.


for (var i = 0; i < array_length_1d(argument0); i += 1) {
    argument0[i] += 1;
}
return argument0;